{
  "name": "evntboard-plugin-sample",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/index.js",
  "scripts": {
    "watch": "ncc build src/index.ts -w -o dist",
    "build": "ncc build src/index.ts -m -o dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/EvntBoard/plugin-sample.git"
  },
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/EvntBoard/plugin-sample/issues"
  },
  "homepage": "https://github.com/EvntBoard/plugin-sample#readme",
  "devDependencies": {
    "@types/node": "^14.14.25",
    "typescript": "^4.1.3"
  },
  "evntboard": {
    "multipleInstances": false,
    "config": {
      "answer": 42
    },
    "schema": {
      "type": "object",
      "properties": {
        "answer": {
          "description": "answer ?",
          "type": "string"
        }
      },
      "required": [
        "answer"
      ]
    }
  }
}
